name: Python SonarQube Analysis

on: [push]

jobs:
  sonarqube-analysis:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run SonarQube analysis
      run: |
        sonar-scanner \
          -Dsonar.projectKey=brunocaxias_test-selenium-cucumber \
          -Dsonar.organization=serra-para-todos \
          -Dsonar.sources=.

    - name: Upload SonarQube analysis results
      uses: sonarsource/sonarcloud-github-action@master
      with:
        github_token: ${{ afsçkqoufiquwbfuiqfbdasd1çi3hr813gfonf }}
        sonar_token: ${{ 44116a96832124a0d0bd6996abf9ac1ca0cc9e4e }}